from setuptools.errors import RemovedCommandError
import logging


class register:
    """Removed distutils register command."""

    def run(self):
        msg = (
            "The distutils register command has been removed, use twine to upload "
            "instead (https://pypi.org/p/twine)"
        )
        logging.error(msg)
        raise RemovedCommandError(msg)
