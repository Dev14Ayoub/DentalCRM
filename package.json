{
  "scripts": {
    "build:css": "sass --quiet --no-source-map --style=compressed --load-path=node_modules dental/static/scss/custom-bootstrap.scss dental/static/css/styles.css",
    "build": "npm run build:css",
    "watch:css": "sass --watch --load-path=node_modules dental/static/scss:dental/static/css"
  },

  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.0",
    "copyfiles": "^2.4.1",
    "sass": "^1.66.1"
  }
}

